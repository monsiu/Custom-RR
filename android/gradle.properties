org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
# Note: if your IDE picks a JDK that Gradle 8.14 does not support (e.g. JDK 26),
# pin it locally by adding `org.gradle.java.home=/path/to/jdk21` to
# ~/.gradle/gradle.properties (user-scope, untracked). Do not commit that here,
# the path differs between machines and breaks CI.
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
