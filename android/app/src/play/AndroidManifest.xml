<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<!-- Google Play variant manifest. -->
<!-- -->
<!-- This is used ONLY when the build is invoked with PLAY_BUILD=true (see -->
<!-- android/app/build.gradle, which swaps the main manifest for this one). -->
<!-- Every other build (the default `flutter build apk`, the GitHub release -->
<!-- workflow, and the F-Droid recipe) keeps src/main/AndroidManifest.xml -->
<!-- untouched. -->
<!-- -->
<!-- Differences from the main manifest: -->
<!--  * No REQUEST_INSTALL_PACKAGES. The Play build ships without the in-app -->
<!--    APK updater (compiled out via the PLAY_BUILD dart-define), and Google -->
<!--    Play's policies disallow self-updating / APK side-loading. -->
<!--  * No package-archive query intent (the installer is gone). -->
<!--  * No crypto wallet URI scheme query intents. The Play build hides the -->
<!--    in-app crypto donation UI to stay clear of Play's financial-services -->
<!--    policy, so those wallet deep-links are never launched. -->

<uses-permission android:name="android.permission.INTERNET" />

    <!-- Provide required visibility configuration for API level 30 and above -->
<queries>

    <!-- to open https URLs -->
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="https" />
    </intent>
  <!-- If your app checks for call support -->
  <intent>
    <action android:name="android.intent.action.DIAL" />
    <data android:scheme="tel" />
  </intent>
  <intent>
    <action android:name="android.intent.action.SEND" />
    <data android:mimeType="*/*" />
  </intent>
</queries>
   <application
        android:label="Custom RR"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
</manifest>
