# F-Droid build recipe for Custom RR.
#
# This file is NOT used by the app build. It is the metadata recipe that
# goes into F-Droid's data repo. To submit:
#
#   1. Fork https://gitlab.com/fdroid/fdroiddata
#   2. Copy this file to  metadata/io.github.monsiu.custom_rr.yml
#   3. Run  fdroid lint io.github.monsiu.custom_rr  and
#           fdroid build io.github.monsiu.custom_rr  locally if you can
#   4. Open a merge request.
#
# Lines marked "VERIFY" below should be double-checked against the actual
# tagged release before submitting.

Categories:
  - System
License: GPL-3.0-only
AuthorName: monsiu
AuthorWebSite: https://monsiu.github.io/
SourceCode: https://github.com/monsiu/Custom-RR
IssueTracker: https://github.com/monsiu/Custom-RR/issues
Changelog: https://github.com/monsiu/Custom-RR/blob/HEAD/CHANGELOG.md
# VERIFY: donation link must be confirmed by upstream (it is: same GitHub account).
Donate: https://buymeacoffee.com/monsiutech
Bitcoin: bc1qaxx6dxkz0s5cw4h9nysw4yvmsaf3qlk7j0gwa2
Litecoin: ltc1qdrjqjzk0sfn7grysxruxuuev6jpn9yqm8wrrg0

AutoName: Custom RR

RepoType: git
Repo: https://github.com/monsiu/Custom-RR.git

Builds:
  - versionName: 0.2.3
    versionCode: 6001
    commit: 33afa5c5ecc078ec6c0933210f84abc7bafaee15
    output: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
    srclibs:
      - flutter@stable
    rm:
      - ios
      - linux
      - macos
      - web
      - windows
    prebuild:
      - export PUB_CACHE=$(pwd)/.pub-cache
      - export flutterVersion=$(cat .flutter-version)
      - git -C $$flutter$$ checkout -f $flutterVersion
      - $$flutter$$/bin/flutter config --no-analytics --no-cli-animations
      - $$flutter$$/bin/flutter pub get --enforce-lockfile
    scandelete:
      - .pub-cache
    build:
      - export PUB_CACHE=$(pwd)/.pub-cache
      - $$flutter$$/bin/flutter build apk --split-per-abi --target-platform=android-arm
        --release --dart-define=FDROID_BUILD=true

  - versionName: 0.2.3
    versionCode: 6002
    commit: 33afa5c5ecc078ec6c0933210f84abc7bafaee15
    output: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
    srclibs:
      - flutter@stable
    rm:
      - ios
      - linux
      - macos
      - web
      - windows
    prebuild:
      - export PUB_CACHE=$(pwd)/.pub-cache
      - export flutterVersion=$(cat .flutter-version)
      - git -C $$flutter$$ checkout -f $flutterVersion
      - $$flutter$$/bin/flutter config --no-analytics --no-cli-animations
      - $$flutter$$/bin/flutter pub get --enforce-lockfile
    scandelete:
      - .pub-cache
    build:
      - export PUB_CACHE=$(pwd)/.pub-cache
      - $$flutter$$/bin/flutter build apk --split-per-abi --target-platform=android-arm64
        --release --dart-define=FDROID_BUILD=true

  - versionName: 0.2.3
    versionCode: 6003
    commit: 33afa5c5ecc078ec6c0933210f84abc7bafaee15
    output: build/app/outputs/flutter-apk/app-x86_64-release.apk
    srclibs:
      - flutter@stable
    rm:
      - ios
      - linux
      - macos
      - web
      - windows
    prebuild:
      - export PUB_CACHE=$(pwd)/.pub-cache
      - export flutterVersion=$(cat .flutter-version)
      - git -C $$flutter$$ checkout -f $flutterVersion
      - $$flutter$$/bin/flutter config --no-analytics --no-cli-animations
      - $$flutter$$/bin/flutter pub get --enforce-lockfile
    scandelete:
      - .pub-cache
    build:
      - export PUB_CACHE=$(pwd)/.pub-cache
      - $$flutter$$/bin/flutter build apk --split-per-abi --target-platform=android-x64
        --release --dart-define=FDROID_BUILD=true

MaintainerNotes: |-
  Flutter app. Built with the F-Droid flutter srclib on the stable channel,
  checked out to the exact version pinned in the repo's .flutter-version file
  (extracted in prebuild).

  ABI split: the app ships one APK per ABI (flutter build apk --split-per-abi).
  android/app/build.gradle assigns each split a distinct versionCode using the
  stock Flutter scheme base * 1000 + abiCode, which keeps the ABI in the
  low-order digits (armeabi-v7a -> x001, arm64-v8a -> x002, x86_64 -> x003) and
  matches the VercodeOperation below. The base versionCode comes from
  pubspec.yaml's +N build number (6 for 0.2.3), so the embedded codes are
  6001/6002/6003.

  The --dart-define=FDROID_BUILD=true flag compiles out the app's self-update
  machinery (see lib/util/build_flags.dart): no GitHub release polling, no
  in-app APK download/install path, and no "Check for updates" UI. The
  GitHub-release variant keeps it; the F-Droid variant does not, so there is
  no UpdateCheck anti-feature and no APK side-loading.

  Signing: android/app/build.gradle falls back to debug signing when
  android/key.properties is absent (it is not committed), so the build
  produces an APK that F-Droid signs with its own key.

AutoUpdateMode: Version
UpdateCheckMode: Tags ^v[0-9.]+$
VercodeOperation:
  - 1000 * %c + 1
  - 1000 * %c + 2
  - 1000 * %c + 3
UpdateCheckData: pubspec.yaml|version:\s.+\+(\d+)|.|version:\s(.+)\+
CurrentVersion: 0.2.3
CurrentVersionCode: 6003
